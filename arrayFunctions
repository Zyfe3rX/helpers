//check if index exists
let isIndexValid = array.indices.contains(index) 

// Finds indexes of all occurances of said element - 0 & 4
let array = ["A", "Z", "C","A"]
let indexes = array.enumerate().filter {
    $0.element.containsString("A")
    }.map{$0.index}
print(indexes) 

//Finds first index where the item is
array.firstIndex(where: {$0 == "person1"})

//Find lowest number in array
let list = [5, 4, 3, 5, 2, 50, 8, 10, 300]
let minValue = list.min()
